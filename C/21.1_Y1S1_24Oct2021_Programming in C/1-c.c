#include <stdio.h>

int main() {
    /*
    A translator is a program that converts source code into machine code.

    3 types of translators,
        Compiler -  A program that converts high-level code into machine code.
        interpreter - A program that translates and executes high-level code one statement at a time.
        Assembler - A program that converts assembly language into machine code.
    */
}
